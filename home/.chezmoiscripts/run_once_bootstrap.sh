#!/bin/sh
# Runs once per machine on first `chezmoi apply`.
#
# Placeholder for machine bootstrap — intended future contents:
#   - install Homebrew if missing
#   - `brew bundle` against a checked-in Brewfile
#   - macOS defaults writes
exit 0
