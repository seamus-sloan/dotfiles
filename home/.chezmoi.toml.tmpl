# Rendered into ~/.config/chezmoi/chezmoi.toml by `chezmoi init`. Records where
# this repo was cloned, so plain `chezmoi` commands find it without --source.
sourceDir = {{ .chezmoi.workingTree | quote }}
